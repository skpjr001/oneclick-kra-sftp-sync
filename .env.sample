# SFTP Sync Configuration via Environment Variables
# Copy this file to .env and modify the values as needed

# Source SFTP Server Configuration
SOURCE_HOST=source.example.com
SOURCE_PORT=22
SOURCE_USERNAME=sourceuser
SOURCE_PASSWORD=sourcepass
# SOURCE_KEYFILE=/path/to/private/key
SOURCE_TIMEOUT=30
SOURCE_KEEPALIVE=30

# Destination SFTP Server Configuration
DEST_HOST=dest.example.com
DEST_PORT=22
DEST_USERNAME=destuser
DEST_PASSWORD=destpass
# DEST_KEYFILE=/path/to/private/key
DEST_TIMEOUT=30
DEST_KEEPALIVE=30

# Sync Configuration
SOURCE_PATH=/source/root
DEST_PATH=/dest/root
EXCLUDE_PATTERNS=.tmp,.lock,.part,.DS_Store
MAX_CONCURRENT_TRANSFERS=10
CHUNK_SIZE=65536
RETRY_ATTEMPTS=3
RETRY_DELAY=5
VERIFY_TRANSFERS=true
DAYS_TO_SYNC=1

# Usage Notes:
# - Either PASSWORD or KEYFILE must be provided for each SFTP connection
# - EXCLUDE_PATTERNS should be comma-separated (no spaces)
# - CHUNK_SIZE is in bytes (65536 = 64KB)
# - RETRY_DELAY is in seconds
# - VERIFY_TRANSFERS: true/false
# - DAYS_TO_SYNC: number of days to sync from today backwards
